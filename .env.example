# Identifiants /admin (optionnel : par défaut camille123 / camille123 dans le code)
# VITE_ADMIN_IDENTIFIER=
# VITE_ADMIN_PASSWORD=

# POST JSON (chaque événement) — optionnel, pour votre API / serverless
# VITE_ANALYTICS_INGEST_URL=
